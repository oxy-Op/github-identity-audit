# Synthetic historical disclosure fixture

The test harness should commit a file containing `Contact Sasha`, remove it in a later commit, and verify that mirror history still reports the original blob.
